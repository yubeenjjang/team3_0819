# Starter

다음 순서로 빈 함수를 완성합니다.

1. `chunking.py`: 문서를 문장 단위 Chunk로 나눕니다.
2. `retrieval.py`: 질문과 겹치는 단어로 검색 점수를 계산합니다.
3. `grounded_answer.py`: 검색 결과가 없을 때 답변을 제한합니다.

각 파일은 독립 실행할 수 있습니다. 완성 후 `solution/README.md`에서 완성본 위치를 확인합니다.
