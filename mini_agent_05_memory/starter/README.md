# Starter · Memory

1. `conversation_window.py`: 최근 메시지만 선택합니다.
2. `memory_store.py`: 사용자별 CRUD와 격리를 구현합니다.
3. `relevant_memory.py`: 질문에 관련된 Memory만 선택합니다.

각 파일은 Docker 없이 작성할 수 있습니다. 시간이 부족하면 `solution` 안내의 완성본으로 수업을 이어갑니다.
